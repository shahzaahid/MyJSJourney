function addTwoNumber(number1, number2) {
  if (number1 && number2 === NaN) {
    console.log("Enter the number");
  } else return number1 + number2;
}
//in the above example numer1, number2 are callled the parameters

addTwoNumber(10, "20");
// in the above example 10, 20 are called the arguments
